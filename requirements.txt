# German Flashcard System Dependencies

# Anki deck generation
genanki>=0.13.0

# Deck unpacking (Anki 2.1.50+ uses Zstandard compression)
zstandard>=0.18.0

# Claude API SDK (optional — used by card_generator.py when ANTHROPIC_API_KEY is set)
anthropic>=0.40.0

# Claude CLI wrapper (fallback when ANTHROPIC_API_KEY is not set)
claude-runner>=0.1.0

# JSON Schema validation (used by card_generator.py)
jsonschema>=4.0.0

# Piper TTS for audio generation (audio/generated_audio/scripts/generate_audio.py).
# Requires Python ≤3.12 — onnxruntime/piper-phonemize lack 3.14 wheels.
piper-tts>=1.4.0

# Local config file support (.env) — ANTHROPIC_API_KEY and other secrets
python-dotenv>=1.0.0

# Testing framework
pytest>=7.0
