# Copy this file to .env and fill in your values.
# .env is gitignored and will never be committed.

# Anthropic API key — used by card_generator.py when ANTHROPIC_API_KEY is set.
# If not set, the script falls back to the 'claude' CLI.
ANTHROPIC_API_KEY=
